# TODO — 커스텀 기능 로드맵 (fork: agopwns)

작성: 2026-07-10. 우선순위 순.

## 진행 예정 — v1.8 배치 (2026-07-10, Codex Sol 기획 · 추천 순서대로)

- [x] **사전 보완** (v1.7.4) — `usage-history.json` 읽기/upsert 분리(`getClaudeModels()` 실패 시에도 히스토리 렌더). 리셋 순간 이벤트 파일은 펫 이벤트 버스에서 함께 구현
- [x] **즉시 렌더 수집 캐시 + 데이터 헬스 센터** (v1.8.0) — 렌더는 `.usage-snapshot.json` 캐시로 즉시(2.4s→0.04s), 수집(ccusage×2·OAuth)은 `--collect` 백그라운드 spawn 분리. 임시파일+rename 원자 교체, lock TTL 90s, 📡 데이터 신선도 행 + "지금 재수집" 액션, burn 샘플은 measuredAt 기준 중복 방지
- [x] **쿼터 페이스 코치** (v1.9.0) — 실제 %p/h vs 안전 %p/h(잔량÷리셋까지 시간) 비교, safe/hot(0.85~1.15 완충)/critical 행 + "N×로 줄여야" 힌트, critical 2회 연속일 때만 알림(히스테리시스), `.batt-burn.json`에 `pace` 필드로 펫과 공유
- [x] **월말 랜딩 예측 + 세이프 스펜드** (v1.10.0) — 최근 14일 완료일 중앙값 페이스로 "월말 예상 $X (페이스 $P/일) · 예산 ±$Y · 남은 하루 $Z" 행, 초과 페이스 최초 진입 시 월 1회 알림(래칫, `.batt-landing-state.json`). 완료일 5개 미만이면 숨김
- [x] **런어웨이 지출 감지** (v1.11.0) — 최근 28 완료일 max(P90, 중앙값+k×MAD)+절대 초과액 기준 오늘 지출 이상치 판정, ⚠ 행 + 하루 1회 알림(`.batt-anomaly-state.json`), 최고 비용 모델 비중 힌트. 감도 `.batt-anomaly` off/normal(k=3,+$25)/sensitive(k=2,+$10) 설정 서브메뉴
- [x] **펫 이벤트 버스 + 종별 희귀 행동** (v1.12.0) — 본체가 `.pet-events.json` 링(10개)에 quota-reset·budget-over·spend-anomaly 발행(알림 off와 독립), 펫이 `.pet-event-seen.json` 중복 방지 후 8초 이벤트 애니메이션(기존 스프라이트 재조합: 리셋→party, 예산→tired/exhausted 교대, 폭주→고속 질주). 희귀 idle 행동 4종 신규 2프레임(고양이 세수·햄스터 볼 빵빵·거북이 등껍질·토끼 빙키 점프) — 15분 버킷 결정적 재생(평균 45분당 12초)

### 백로그 (이번 배치 제외)

- [x] **모델 믹스 트렌드** (v1.13.0) — 최근 7일 vs 직전 7일 계열별(Fable/Opus/Sonnet/Haiku/GPT 정규화) 비용 비중 상위 3개 접힌 서브메뉴, ↑↓%p 변화 색상(증가=주의색). 데이터일 3개 미만이면 숨김
- [x] **리셋 타임라인 + 리셋 임박 알림** (v1.14.0) — 드롭다운 상단 "다음 회복 C5 28m → X5 1h12m → …" 임박 순 한 줄(Codex 3h+ 스테일 제외), 잔량 20% 이하 배터리는 리셋 15분 전 1회 알림(`.batt-reset-reminder-state.json`에 리셋 시각당 1회 래칫)

## 완료

- [x] **픽셀 펫** (v1.7.0) — 별도 스트리밍 플러그인 `claude-pet.streamable.js`: 고양이/햄스터/거북이/토끼 4종×9프레임, `.batt-burn` 연동 상태머신(작업중=달리기·유휴·잠·지침·방전·리셋 파티), 렌더 캐시로 CPU ~0, `.pet-species` 선택, 켜기/끄기=파일명 `.off` 토글
- [x] **스타일 테마** (v1.7.0) — 채움 팔레트 green/neon 추가 + 원클릭 프리셋 4종(미니멀/신호등 클래식/게임보이/네온)
- [x] **펫 말풍선** (v1.7.0) — 드롭다운 상단 💬 데이터 기반 코멘트 (방전 페이스>예산 초과>쿼터 부족>풀충전>열일 중>기본, 우선순위 분기)
- [x] **초저폭 텍스트 모드** (v1.6.0) — `.batt-display`=image/text, 텍스트 모드 `C43·88 X96` (SHOWSET 반영, Menlo 고정폭), 서브메뉴 "표시 방식" 토글
- [x] **7일 지출 스파크라인** (v1.6.0) — 드롭다운 "최근 7일 $합계 · 최대 $X/일" 행 + 36×16pt 바차트(오늘=강조색, 무데이터=스텁)
- [x] **월 예산 게이지** (v1.6.0) — `.batt-budget`(USD, 수동 설정) 있으면 "이번 달 누적 $X / 예산 $Y (Z%)" 색상 게이지 + 80/100% 상향 돌파 월 1회 알림
- [x] **일별 사용량 자동 스냅샷** (v1.5.0) — `usage-history.json` 날짜별 upsert(400일 보관), 드롭다운 "이번 달 누적 $X · 지난달 $Y" 행. ccusage 30일 보존 한계 해결
- [x] **임계값·리셋 알림** (v1.5.0) — 20%/10% 하향 돌파·리셋 회복(≥95%) macOS 알림, `.batt-notify` on/off 토글, Codex 3h+ 스테일 제외, zone 상태 파일로 중복 방지
- [x] **소진 예측 경고** (v1.5.0) — C5 잔량 90분 샘플 기울기로 투영, 리셋 전 소진 페이스면 빨간 "예상 소진 HH:MM" 행 + 블록당 1회 알림
- [x] pHYs DPI 표시 크기 제어 + 전체 크기 % 스테퍼 (50~200%)
- [x] 숫자 단일색 렌더 + 빈 구간 반투명 트랙 (경계 분할 가독성 문제 해소)
- [x] 채움 색 / 글자 색 / 글자 크기 옵션
- [x] 배터리별 표시 on/off 토글 (.batt-show)
- [x] ⚙️ 배터리 설정 서브메뉴 통합
- [x] 포크 전환 (self-update → agopwns, v1.4.0)
